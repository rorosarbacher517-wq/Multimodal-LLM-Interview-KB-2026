# 05_SFT后视觉能力下降为什么

## 面试一句话

常见原因不是“模型变笨”，而是 SFT 数据分布让模型过度拟合语言格式、弱化了视觉依赖。

## 核心回答

- 文本数据占比太高，模型学会靠语言先验回答。
- 视觉输入被低分辨率/过度压缩。
- 只优化最终答案，缺少 grounding/perception 监督。
- 学习率过大导致 vision-language alignment 被破坏。
- 解决方案：混入预训练数据、分桶采样、较小 LR、冻结部分模块、加视觉 verifier。
