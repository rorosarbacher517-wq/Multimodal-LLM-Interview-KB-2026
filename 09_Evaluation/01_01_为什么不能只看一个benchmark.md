# 01_为什么不能只看一个benchmark

## 面试一句话

多模态能力是多维的：通用理解、OCR、数学图表、grounding、视频、agent、幻觉可能互相独立。

## 核心回答

- 一个模型 MMMU 高，不代表 OCR/GUI/Video 同样强。
- 要按能力桶建立 benchmark matrix。
- 同一 benchmark 还要注意 prompt、CoT、sampling、image resolution 是否一致。
